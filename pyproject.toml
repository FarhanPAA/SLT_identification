[project]
name = "slt"
version = "0.1.0"
description = "Single Lowest Tender (SLT) calculator (Streamlit UI + Python module)."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "streamlit>=1.52.2",
]

[build-system]
requires = ["hatchling>=1.26.0"]
build-backend = "hatchling.build"
